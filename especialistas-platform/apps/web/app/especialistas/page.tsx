'use client';
import {useEffect,useState} from 'react'; import {API} from '../../lib/api';
export default function Specialists(){const [items,setItems]=useState<any[]>([]);useEffect(()=>{fetch(API+'/specialists').then(r=>r.json()).then(setItems).catch(()=>setItems([]))},[]);return <main className="wrap"><h1>Especialistas</h1><p className="muted">Directorio público inicial.</p><div className="grid">{items.map(s=><div className="card" key={s.id}><h3>{s.user?.name}</h3><p>{s.description||'Perfil nuevo'}</p><p>Experiencia: {s.experienceYears} años</p><p>Confianza: {s.trustScore}%</p><p>{s.categories?.map((x:any)=>x.category.name).join(', ')||'Sin categorías todavía'}</p></div>)}{!items.length&&<div className="card">Todavía no hay especialistas registrados.</div>}</div></main>}
