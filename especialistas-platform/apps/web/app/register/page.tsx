'use client';
import {useState} from 'react'; import {api} from '../../lib/api';
export default function Register(){const [m,setM]=useState(''); async function submit(e:any){e.preventDefault();const f=new FormData(e.currentTarget);try{const r=await api('/auth/register',{method:'POST',body:JSON.stringify(Object.fromEntries(f))});localStorage.setItem('token',r.accessToken);location.href='/dashboard'}catch(err:any){setM(err.message)}}return <main className="wrap"><div className="card"><h1>Crear cuenta</h1><form onSubmit={submit}><input name="name" placeholder="Nombre completo" required/><input name="email" type="email" placeholder="Correo" required/><input name="phone" placeholder="Teléfono"/><input name="password" type="password" minLength={8} placeholder="Contraseña (mínimo 8 caracteres)" required/><select name="role"><option value="CLIENT">Quiero contratar servicios</option><option value="SPECIALIST">Quiero ofrecer mis servicios</option></select><button>Crear cuenta</button></form>{m&&<p className="error">{m}</p>}</div></main>}
