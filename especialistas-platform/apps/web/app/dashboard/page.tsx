'use client';
import {useEffect,useState} from 'react'; import {api} from '../../lib/api';
export default function Dashboard(){const [u,setU]=useState<any>(null);const [err,setErr]=useState('');useEffect(()=>{api('/users/me').then(setU).catch(e=>setErr(e.message))},[]);if(err)return <main className="wrap"><p className="error">{err}. Inicia sesión primero.</p></main>;if(!u)return <main className="wrap"><p>Cargando...</p></main>;return <main className="wrap"><div className="card"><h1>Hola, {u.name}</h1><p><span className="badge">{u.role}</span></p>{u.role==='SPECIALIST'?<><p>Tu perfil profesional ya existe. En Sprint 2 puedes completarlo mediante la API y después añadiremos el formulario visual completo.</p><a href="/especialistas">Ver directorio público</a></>:<p>Tu perfil de cliente está listo. En Sprint 3 añadiremos la publicación de solicitudes de trabajo.</p>}<button onClick={()=>{localStorage.removeItem('token');location.href='/'}}>Cerrar sesión</button></div></main>}
